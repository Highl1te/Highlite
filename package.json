{
  "name": "highlite",
  "version": "0.1.4",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/tampermonkey": "^5.0.4"
  },
  "scripts": {
    "build": "bun build src/loader.ts --outdir=dist --define:import.meta.env.NODE_ENV='\"production\"'",
    "test": "bun test"
  }
}
